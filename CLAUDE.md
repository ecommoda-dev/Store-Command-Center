<div dir="rtl">

# CLAUDE.md — دليل الشغل على الريبو ده

الملف ده لأي جلسة جاية (Claude Code أو Cowork) هتلمس الأداة دي. اقراه قبل أول تعديل.

---

## اقرا الأول — بالترتيب ده

1. **`docs/DATA-CONTRACT.md`** — المرجع الحاكم. كل رقم له سطر بمعادلته ومصدره
   وحدوده. أي تعديل بيخالفه بيطلّع **رقم غلط شكله صح** — وده أسوأ من أي عطل ظاهر.
2. `README.md` — البنية ومبادئ التصميم.
3. `docs/IMPROVEMENTS.md` — اللي **متعمّد** إنه مش موجود (وليه) مقابل اللي ناقص فعلاً.
   قبل ما تضيف ميزة، شوف هي هناك ولا لأ — فيه بنود مرفوضة بمبرر مكتوب.

---

## القواعد اللي متتكسرش

| القاعدة | ليه |
|---|---|
| **مفيش Cron في `wrangler.toml`** | كل نداء لشوبيفاي = فعل بشري صريح. الجدولة = استهلاك حصة وطابور فشل صامت |
| **`productVariants(query:)` ممنوعة** | الفلتر **بيتجاهل بصمت** — بيرجّع كل الفاريانتس وكأنه فلتّر. الدليل في عقد البيانات §5 |
| **مفيش كتابة كاش جزئية** | الكتابة بعد آخر صفحة تنجح بس. أي صفحة تفشل = الطلب كله يفشل |
| **مفيش إعادة قراءة من KV بعد الكتابة** | نافذة الاتساق ٦٠ ثانية — القراءة الفورية بترجّع القديم |
| **`CACHE_VERSION` يزيد مع أي تغيير في الحقول** | من غير كده، بيانات قديمة بشكل جديد = انهيار صامت |
| **الأداة قراءة فقط** | ممنوع أي mutation على شوبيفاي. السجل بيتكتب `login`/`logout` بس |
| **مفيش نص مستخدم في SQL** | فلاتر السجل placeholders، وأعمدة الترتيب whitelist |
| **الحالة مبتتخزنش على المتصفح** | `currentEmployee` في ذاكرة الصفحة بس. الإعدادات بس هي اللي في `localStorage` |

---

## الفخاخ اللي اتلسعنا منها فعلاً (متكررهاش)

### 1. الطرح الكامل للكمية غير المسوّاة

```js
// ❌ غلط: على سطر كميته 2 ورجع منه واحد، شوبيفاي بيخلي C=1 و unsettledQty=1،
//    فالطرح الكامل بيلغي القطعة المدفوعة الباقية ويخفي قيمتها من كل الأرقام
const aliveQty = C - unsettledQty;

// ✅ صح: اطرح التداخل بس
const overlap  = Math.max(unsettledQty - goneTotal, 0);
const aliveQty = Math.max(C - overlap, 0);
```

### 2. صيغتان للوقت في نفس عمود D1

`metafields_change` بيكتب `"2026-08-23 - 23:53:32"` (القاهرة، صيغة مخصّصة)، وكل
الباقي ISO UTC. أي استعلام لازم يستخدم:

```sql
COALESCE(substr(datetime(substr(timestamp,1,19),'+3 hours'),1,10), substr(timestamp,1,10))
```

من غير الزحلقة، كل نشاط من ١٢ لـ ٣ الفجر بيتحسب على اليوم اللي فات.

### 3. مستندات المهارات ممكن تكون قديمة — اتأكد من القاعدة الحيّة

اتأكدنا بالاستعلام على D1 الحقيقي (23-08-2026):
- العمود **`item_count` موجود** رغم إن `d1-schema.md` مش ذاكره.
- **`tool='pack_checker'` بيرجّع صفوف** رغم إن `ecommoda-constants` بيقول `pack_verification`.
- الحل المطبَّق: `IN ('pack_checker','pack_verification')` — القيمتين، كتأمين.

### 4. الرسم جوّه `try` بتاع تحميل البيانات

لو `renderActiveTab()` اتنادت جوّه `try` بتاع `loadAll`، أي استثناء في أي renderer
بيتصاد في نفس الـ `catch` ويتعرض للمدير على إنه **«فشل تحميل البيانات»** — كذبة
كاملة. الرسم دلوقتي **برّه** الـ try، وعطل الرسم بيتعرض جوّه التاب نفسه (`panelError`).

### 5. علامة «التاب اترسم» قبل الرسم

`state.renderedTabs[t] = true` لازم تتحط **بعد** نجاح الرسم. لو اتحطت قبله، تاب
فشل رسمه مرة بيفضل فاضي للأبد لأن كل نداء بعد كده بيعمل return مبكر.

### 6. قيمة بيانات جوّه سمة `onclick`

**ممنوع**. `esc()` لوحدها **مش** بتحل: `&#39;` بيترجع `'` قبل ما محرك الـ JS يشوفه.
الحل: مرّر **رقم/فهرس** وهات القيمة من الحالة (`msChipRemove(key, fk, idx)`).

### 7. مكتبات الـ CDN ممكن تكون محجوبة

`Chart.js` و`ExcelJS` بييجوا من CDN. لو اتحجبوا، الكود القديم كان بيرمي
`Chart is not defined` جوه دالة الرسم، والاستثناء ده كان بيتعرض كـ«فشل تحميل
البيانات» — كذبة. دلوقتي `libMissing()` / `ensureChartLib()` بيخلوا الغياب يوقف
**الرسم بس**، والأرقام والجداول بتفضل شغّالة مع بانر بيقول السبب.

### 8. الترتيب `[...rows].sort()` مش `rows.sort()`

`.sort()` بتعدّل المصفوفة الأصلية، وساعتها حالة «بلا ترتيب» مش هترجع للترتيب
الأصلي أبدًا.

---

## المعايير الملزمة (من مهارة `ecommoda-html-builder`)

- **معيار الجداول الموحّد**: كارت واحد (فلاتر multi-select → chips → صف النتائج →
  الجدول)، وترتيب **٣ حالات: تصاعدي ▲ → تنازلي ▼ → بلا ترتيب**.
  كل جداول الأداة بتتبني من `createTable()` — **متبنيش جدول بإيدك**.
- **تاب السجل بيستخدم نفس الكومبوننت في «وضع السيرفر»** (`cfg.server`): نفس الشكل
  والسلوك، بس الفلترة والترتيب بيحصلوا في SQL. ده مش تحسين أداء — الجدول مقسّم
  صفحات على السيرفر، فالفلترة على الصفحة كانت بتخلي عدّاد النتائج والتصدير **كدب**.
- **سلّم z-index**: login = 500 · أي overlay = 600 · toast = 9999 · محتوى ≤ 200.
- **عرض الأداة** Tier L = 1400px (فيها جداول وسجل).
- **الأرقام `en-US`** والتوقيت القاهرة UTC+3. خط واحد: Noto Sans Arabic.

### ⚠️ الفحص الآلي إلزامي قبل أي تسليم

في `ecommoda-html-builder` §Step 9 سكربت bash لازم **يتنفّذ فعليًا** على
`index.html` النهائي (مش يتقرا). أي عدّاد مفروض يكون صفر ورجع رقم = **قف وأصلح**.
اتنفّذ آخر مرة على النسخة الحالية ورجع نظيف.

---

## الاختبارات — شغّلها قبل أي push

```bash
node test/harness.mjs        # 76 اختبار — الأرقام والهوية المحاسبية
node test/make-payload.mjs   # بيولّد test/payload.json من نفس اللقطة
node test/browser-test.mjs   # 75 فحص في متصفح حقيقي
```

**الهوية المحاسبية** اللي لازم تفضل صحيحة في كل تشغيل:

```
القيمة الإجمالية = قيد التنفيذ + الفاقد + صافي المبيعات
```

لو كسرت، **متكملش**. ده مش اختبار تجميلي — ده اللي بيمنع رقم غلط يوصل للمدير.

**بيانات الاختبار** في `/root/work/data/` (لقطة حقيقية: 1,516 أوردر · 276 منتج).
مش في الريبو لأنه **عام**. لو مش موجودة، اجلبها من شوبيفاي بنفس شكل استعلام
عقد البيانات §3 و§5، أو حدّد مسار تاني بـ `SCC_DATA=/path`.

---

## بيئة الاختبار في الجلسات السحابية

- Chromium متثبّت: `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`
  (أو `SCC_CHROME=/path`). شغّله بـ `--no-sandbox`. **متعملش `playwright install`**.
- الـ CDN محجوب في الساندبوكس — الاختبار بيقدّم بدائل محلية بسيطة لـ `Chart`
  و`ExcelJS` عشان يختبر **مسار الكود بتاعنا**، مش المكتبة.
- `window.state` مش معرّف: `const` على مستوى السكربت مش بيبقى خاصية على `window`.
  في `page.evaluate` استخدم `state` مجرّدة.

---

## بصمة المهارات — `index.js`

| المهارة | الإصدار | آخر تحديث |
|---|---|---|
| `ecommoda-worker-builder` | v3.8.0 | الحارس الديناميكي لقيم اللوج (الطبقة ٥ — Step 7-ج) |
| `ecommoda-constants` | v3.1.0 | جدول `log_value_alerts` (§2) وحقل `tool` لكل قيمة في `log-values.json` (§7.1) |

لو الجدول ده مختلف عن سطر `// skills:` أعلى `index.js`، ده مش خطأ مطبعي —
معناه تعديل سابق خلص من غير ما يكمّل الإجراء. صلّح المصدرين مع بعض.

---

## الحدود المعلنة (اقرا قبل ما تضيف رقم جديد)

الأداة **مش** شايفة: مبيعات StyleBox · الزيارات ومعدل التحويل · مصاريف الإعلانات ·
تكلفة الشحن الفعلية · المنتجات المؤرشفة. كل واحدة منهم مكتوبة في الشاشة نفسها
وفي `IMPROVEMENTS.md` بخطة تنفيذ. **متضيفش رقم بيدّعي إنه بيغطي واحدة منهم من
غير المصدر الحقيقي.**

</div>
